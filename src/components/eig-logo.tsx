export const EIGLogo = ({ className }: { className?: string }) => {
  return (
    <div className={`flex items-center justify-center bg-primary rounded-md ${className}`}>
      <span className="font-bold text-lg text-primary-foreground">EIG</span>
    </div>
  );
};
